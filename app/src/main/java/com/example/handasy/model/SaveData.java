package com.example.handasy.model;

import android.app.Activity;

/**
 * Created by ahmed on 4/4/2017.
 */

public class SaveData  {
    SaveData(Activity activity){/*
        SharedPreferences mPrefs = activity.getSharedPreferences("data",MODE_PRIVATE);
        SharedPreferences.Editor prefsEditor = mPrefs.edit();
        Gson gson = new Gson();
        String json = gson.toJson(ActivitySplashDownload.projectList);
        prefsEditor.putString("projectData", json);
        prefsEditor.commit();*/
    }
    }
